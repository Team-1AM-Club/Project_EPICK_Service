# W3 Phase 4 T058 Readiness Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish one clean W3 owner SHA with the C-01 joint runtime regression, non-root durable deployment package, operator recovery path, and T058 evidence contract requested by W1.

**Architecture:** Keep the accepted C-01 event/receipt/status and Source Authority contracts unchanged. Add an explicit bind host for container networking, SQLite online backup/offline restore tooling, immutable container packaging, and evidence/runbook documentation. W2 remains the replay/snapshot producer and W1 owns the final shared-run orchestration.

**Tech Stack:** Python 3.12, stdlib HTTP/SQLite, Pydantic, pytest, Docker Compose, JSON Schema.

**Spec:** `md/deploy/W1_W2_W3_Phase4_T058_W3_Action_Request_2026-09-25.md` on `origin/codex/008-phase4-us2`

## Global Constraints

- Preserve `w3-c01/0.2-candidate/r2` event, receipt, status and Source Authority behavior.
- Never record raw event bodies, private Job/owner data, DSNs or bearer tokens in T058 evidence.
- Container runs non-root with immutable root filesystem and a durable SQLite volume.
- Remote Source Authority uses HTTPS verification; plaintext remains loopback-only.
- Phase 4/T058 stays open until W1, W2 and W3 execute the same run ID.

## Review Focus

- Container bind address must not weaken Source Authority transport validation.
- Backup must be a consistent SQLite snapshot and restore must refuse overwrite.
- Restriction clear must remain non-READY until exact-key re-index completes.
- Authority false/unavailable/timeout must mutate no event state.
- Evidence format must expose counts and hashes only.

---

### Task 1: C-01 joint runtime and operator boundary

**Files:**
- Create: `tests/integration/test_c01_joint_runtime.py`
- Create: `tests/integration/test_c01_operability.py`
- Create: `src/w3_knowledge/c01/operator.py`
- Modify: `src/w3_knowledge/restriction/http.py`
- Modify: `src/w3_knowledge/c01/http.py`

**Interfaces:**
- Consumes: existing `Store`, `HTTPSourceAuthority`, C-01 HTTP routes.
- Produces: explicit `host` bind option and `python -m w3_knowledge.c01.operator backup|restore`.

- [ ] Add focused tests for Authority false/503/timeout, clear+reindex, replacement, restart, duplicate/gap/conflict/replay/snapshot and private-field rejection.
- [ ] Add failing tests for non-loopback explicit bind and SQLite backup/restore validation.
- [ ] Implement the minimal bind and operator behavior.
- [ ] Run focused tests and commit.

### Task 2: Immutable deployment package and runbook

**Files:**
- Create: `.dockerignore`
- Create: `deploy/Dockerfile`
- Create: `deploy/c01.compose.yml`
- Create: `docs/w3-c01-phase4-runbook-2026-09-25.md`
- Create: `tests/integration/test_c01_deploy_artifacts.py`

**Interfaces:**
- Consumes: Task 1 host option and backup/restore CLI.
- Produces: non-root read-only C-01 image, private network, named SQLite volume, bounded restart and documented operation commands.

- [ ] Add failing artifact tests.
- [ ] Add Dockerfile, Compose and runbook.
- [ ] Run artifact/focused tests and commit.

### Task 3: W1 response and count-only evidence contract

**Files:**
- Create: `contracts/c01/v0.2-candidate/t058-count-evidence.schema.json`
- Create: `contracts/c01/v0.2-candidate/examples/t058-count-evidence.json`
- Create: `docs/w1-w2-w3-phase4-t058-w3-response-2026-09-25.md`
- Modify: `README.md`
- Modify: `contracts/core-runtime/readiness.json`

**Interfaces:**
- Consumes: Task 1/2 files and final test results.
- Produces: reproducible W3 handoff, contract hashes, test mapping and shared-run evidence shape.

- [ ] Validate the schema with its synthetic example and ensure it contains no raw payload fields.
- [ ] Record commands, environment names, ownership boundaries and remaining shared Gate.
- [ ] Run full tests, lint, format, schema/export drift and commit.
