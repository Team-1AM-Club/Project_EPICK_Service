# W2 C01 Implementation Plan

> 실행: superpowers:executing-plans, 현재 승인된 feature branch에서 기존 미커밋 구현을 보존한다.

**Goal:** W2 원본 이벤트를 받는 이중 순번 소비/복구/색인 및 W4 참조 어댑터 제공.
**Architecture:** legacy를 그대로 두고 별도 C01 contracts/store/http 모듈을 추가한다.
**Tech Stack:** Python, Pydantic, jsonschema, SQLite FTS5, pytest.
**Spec:** docs/superpowers/specs/2026-09-16-w2-c01-design.md

- [x] C1: `tests/integration/test_c01.py`에 원본 fixture 입력/오류 테스트부터 작성하고 실패 확인.
  `src/w3_knowledge/c01/contracts.py`: Event.model_validate, Replay, Snapshot, IndexRequest,
  Status, Signal을 구현. 원본 schema와 fixture 복사 및 SHA256 기록.
- [x] C2: 동일 테스트 파일에서 event_cursor=4/restriction_revision=1, gap 복구,
  duplicate/conflict/restart 검증 실패 확인 후 `c01/store.py` Store 구현.
  공개 메서드 consume(Event), replay(Replay), snapshot(Snapshot), status(source).
- [x] C3: 색인/TTL/복수 restriction/W4 테스트 실패 확인 후 index(IndexRequest),
  search(query), signals(), acknowledge(id), purge(), C01Cache 구현.
- [x] C4: 실제 localhost HTTP role/error/recovery 테스트 후 `/c01/v1/*`와 CLI 연결.
  schema export 및 drift 검사, 새 guard 검증, portable smoke 실행.
- [x] C5: 전체 pytest/ruff/export, 코드 리뷰, 회신 문서 및 ZIP+manifest 작성.

검증: 전체 169 passed / 1 skipped, C01 HTTP smoke 18/18, legacy smoke 23/23.
독립 리뷰의 snapshot 누락/ID 재사용/UUID 대소문자/restriction 순번 재사용 4건 수정 및 재검증.
원본7개 SHA256 일치, wheel/sdist 빌드, 압축 해제본 C01 smoke18/18 검증 완료.

대표 검증: `uv run --locked pytest tests/integration/test_c01.py -q`.
기대: fixture 4개 consume 후 (event_cursor, restriction_revision)==(4,1).
기존 규격 테스트는 변경 없이 함께 통과해야 한다.
원본 복사/생성물 외 수정은 apply_patch로 수행한다.
Git commit/push 및 실제 배포는 이번 실행 범위에 포함하지 않는다.
